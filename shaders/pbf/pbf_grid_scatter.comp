void main() {
	uint i = gl_GlobalInvocationID.x;
	if (i >= uint(params.grid_dims.w)) {
		return;
	}
	uint c = cell_index(cell_of(predicted.data[i].xyz));
	uint slot = cell_start.data[c] + atomicAdd(cell_count.data[c], 1u);
	sorted_indices.data[slot] = i;
}
