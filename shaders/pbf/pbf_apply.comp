void main() {
	uint i = gl_GlobalInvocationID.x;
	if (i >= uint(params.grid_dims.w)) {
		return;
	}
	vec3 p = predicted.data[i].xyz + deltas.data[i].xyz;
	predicted.data[i] = vec4(clamp_domain(p), 0.0);
}
