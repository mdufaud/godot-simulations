void main() {
	uint gid = gl_GlobalInvocationID.x;
	if (gid < num_cells()) {
		cell_start.data[gid] += block_sums.data[gid >> 8u];
		// cleared here so grid_scatter's atomicAdd can rebuild per-cell counts
		cell_count.data[gid] = 0u;
	}
}
